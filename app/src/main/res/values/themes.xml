<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HearingTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cloudy</item>
        <item name="colorPrimaryVariant">@color/cloudy</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gold</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.HearingTest" parent="Base.Theme.HearingTest" />
    <style name="DefaultAppBarLayout" parent="Widget.Design.AppBarLayout">
        <item name="backgroundColor">?attr/colorPrimary</item>
    </style>
</resources>